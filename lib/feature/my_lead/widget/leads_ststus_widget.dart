import 'package:flutter/cupertino.dart';

class LeadsStatusWidget extends StatelessWidget {
  const LeadsStatusWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [

      ],
    );
  }
}
